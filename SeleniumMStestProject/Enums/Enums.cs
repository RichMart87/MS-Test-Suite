namespace SeleniumMStestProject.Enums
{
    public enum BrowserType
    {
        Default = 0,
        Chrome = 1,
        Firefox = 2,
        Edge = 3,
        Safari = 4
    }

    // Note: These enum values below are possible values to initialize different test cases
    public enum LanguageType
    {
        Default = 0,
        English = 1,
        Spanish = 2,
        French = 3,
        German = 4
    }

    public enum EnvironmentType
    {
        Default = 0,
        Local = 1,
        Remote = 2,
        Staging = 3,
        Production = 4
    }
}